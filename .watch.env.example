#!/usr/bin/env bash
# Lokal kopieren nach .watch.env und anpassen (nicht committen)

# Optional: externer Upload-Ordner (Outlook/OneDrive/Dropbox)
# Struktur: <UPLOAD_INBOX_DIR>/<ID>/*.jpg
UPLOAD_INBOX_DIR=""
UPLOAD_ARCHIVE_DIR=".tmp/upload_archive"
UPLOAD_MOVE_TO_ARCHIVE=1

# Watcher-Verhalten
WATCH_POLL_SEC=5
WATCH_STABLE_SEC=8
WATCH_FAIL_COOLDOWN_SEC=60
WATCH_DRY_RUN=0
